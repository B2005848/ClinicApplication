import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    height: 200, // Chiều cao của banner
  },
  image: {
    width: width, // Mỗi hình ảnh sẽ chiếm toàn bộ chiều rộng màn hình
    height: 200,
    resizeMode: "cover",
  },
});

export default styles;
